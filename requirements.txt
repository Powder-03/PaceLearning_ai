
# Core Framework
fastapi
uvicorn[standard]
python-multipart
sse-starlette  # Server-Sent Events for streaming

# Database (MongoDB only - PostgreSQL removed)
pymongo
motor  # Async MongoDB driver for sessions and chat storage

# Configuration
pydantic-settings
python-dotenv

# LangChain & AI (Gemini only)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
google-generativeai  # Direct Gemini SDK for test endpoints

# HTTP Client
httpx

# Authentication (Clerk JWT validation)
PyJWT[crypto]  # JWT decoding with RSA support

# Utilities
python-dateutil
